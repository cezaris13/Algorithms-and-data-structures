diff --git a/my_vector/bin/Debug/my_vector b/my_vector/bin/Debug/my_vector
index b4e099f..f79c78e 100755
Binary files a/my_vector/bin/Debug/my_vector and b/my_vector/bin/Debug/my_vector differ
diff --git a/my_vector/main.c b/my_vector/main.c
index 79eaa1a..6fab839 100644
--- a/my_vector/main.c
+++ b/my_vector/main.c
@@ -10,7 +10,7 @@ void test(){
     v.resize(&v,100);
     printf("resized vector\n");
     print_vector(v);
-    for(int i=0;i<1001;i++){
+    for(int i=0;i<15;i++){
         v.push_back(&v,i);
     }
     //example with strings(change print function from (int) to (char*) )
diff --git a/my_vector/vector.c b/my_vector/vector.c
index f6ff3a2..dabd724 100644
--- a/my_vector/vector.c
+++ b/my_vector/vector.c
@@ -43,7 +43,7 @@ int is_full(){//malloc todo
 int vector_resize(vector *v,int size){
     int status=UNDEFINED;
     if(v){
-        if(size>v->capacity && size<=MAX_SIZE){
+        if(size>v->capacity && !is_full()){
             v->capacity=size;
             v->items=realloc(v->items,sizeof(void*)*size);
             status=SUCCESS;
@@ -53,7 +53,7 @@ int vector_resize(vector *v,int size){
 }
 int vector_push_back(vector *v,void *item){
     int status=UNDEFINED;
-    if(v->size+1<=MAX_SIZE){
+    if(!is_full()){
         v->size=v->size+1;
         if(v->size>v->capacity){
             if(vector_resize(v,v->size)==UNDEFINED){
@@ -103,6 +103,7 @@ int vector_free(vector *v){
         v->size=0;
         v->capacity=0;
         free(v->items);
+//        v->items=NULL;
         status=SUCCESS;
     }
     return status;
